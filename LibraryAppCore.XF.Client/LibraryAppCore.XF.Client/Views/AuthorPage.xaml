<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LibraryAppCore.XF.Client.Views.AuthorPage">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <StackLayout x:Name="authorStack">
                <Label Text="AuthorId" />
                <Entry Text="{Binding Path=Model.Id}" FontSize="Medium" />
                <Label Text="Name" />
                <Entry Text="{Binding Path=Model.Name}" FontSize="Medium" />
                <Label Text="Surname" />
                <Entry Text="{Binding Path=Model.Surname}" FontSize="Medium" />
            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Button Text="Add" Command="{Binding ViewModel.SaveAuthorCommand}" CommandParameter="{Binding Model}" TextColor="White" BackgroundColor="#4cae4c" />
                <Button Text="Delete" Command="{Binding ViewModel.DeleteAuthorCommand}" CommandParameter="{Binding Model}" TextColor="White" BackgroundColor="#d9534f" />
                <Button Text="Back" Command="{Binding ViewModel.BackCommand}" TextColor="White" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>